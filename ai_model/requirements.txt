# Core FastAPI dependencies
fastapi>=0.100.0
uvicorn[standard]>=0.20.0
pydantic>=2.0.0

# Language detection (lightweight)
langdetect>=1.0.9

# ML dependencies (optional): comment out to speed up HF build
# --extra-index-url https://download.pytorch.org/whl/cpu
# torch>=2.0.0
# transformers>=4.30.0
# sentence-transformers>=2.2.0

# Data processing (required for lite predictor)
pandas>=2.0.0
# scikit-learn is only needed for full ML mode
# scikit-learn>=1.3.0
